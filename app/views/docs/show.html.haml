.wrapper_with_padding
  #doc_show
    %h1.title= @doc.title
    %p= simple_format(@doc.content)

    .button
      = link_to 'Edit path', edit_doc_path(@doc), class: 'button'
      = link_to 'delete', doc_path(@doc), method: :delete, data: {confirm: "are you sure"}, class: 'button'

